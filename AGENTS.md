# Project Guidelines & Agent Instructions

## Git Push Policy
- **DO NOT push automatically to git.**
- Never execute `git push` automatically or proactively after completing tasks, editing files, or making commits.
- Only run `git push` when the user explicitly requests it in their prompt (e.g. "push to git", "push the code").
